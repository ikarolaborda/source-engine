[package]
name = "source-steamapi"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

# The engine links this module by the name the C++ one had.
[lib]
name = "steam_api"
crate-type = ["cdylib", "rlib"]

[lints]
workspace = true
