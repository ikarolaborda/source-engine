[package]
name = "source-inputsystem"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

# The engine loads this module by the name the C++ one had.
[lib]
name = "inputsystem"
crate-type = ["cdylib", "rlib"]

[dependencies]
source-cppabi = { path = "../source-cppabi" }
source-input = { path = "../source-input" }

[lints]
workspace = true
