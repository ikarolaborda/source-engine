# Rust port status

This file records evidence, not intended completion.

Latest continuation: [2026-09-20 build and runtime validation](validation-2026-09-20.md).
All eight client/server targets have now built with `--rust-engine`; the Rust
startup reads the selected game's `gameinfo.txt`, and direct `+map` Metal smoke
gates pass for Episode One and Episode Two. This does not establish runtime
compatibility for the six games whose content is unavailable.
The next host increment routes generic LZSS/Snappy buffers through Rust and
omits native Snappy from Rust builds; 283 workspace tests and codec differential
checks pass. The dated evidence also records an intermittent ToGL startup
failure and successful guarded repeats, without claiming it fully resolved.
The next filesystem increment moves ordinary relative ZIP/BSP pack reads and
metadata into Rust, removing the legacy-pack precedence bypass. It validates
69,329 entries across 119 installed map ZIPs against Python, and stored/LZMA
fixtures against Python and Source's native SDK. Workspace tests now total 288.
Native mount discovery and absolute/pure-server/filtered read paths remain.
A fresh ToGL check rejected an empty adapter list, then crashed during console
unregistration on shutdown; the earlier guard is not a complete failure-path fix.
The following lifecycle increment identifies the shader unload ordering and
constructor-started texture workers behind that crash. Rust now sequences
synchronous app-system startup/rollback; deterministic shader-init failure
cleans up and returns an error on Metal and ToGL. There are 291 passing workspace
tests plus 1,088 public-ABI startup/rollback cases. A subsequent increment moves
the separate Startup/Shutdown path to the same Rust policy, with opaque lifecycle
handles and stale/reentrant/wrong-owner rejection. There are now 293 passing
workspace tests, 1,088 cases per lifecycle API and 1,000 reentrant split cycles.
Split shader-init failure cleans up on both renderers; a fixed-camera Metal
map-load/exit run also passes, including duplicate shutdown. Individual subsystem
bodies and intermittent ToGL enumeration remain native or unresolved; see dated
evidence for the exact runtime scope. A subsequent full eight-target rebuild
and package audit now refresh that matrix after the codec/pak/lifecycle changes.
All 16 game libraries and 28 shared libraries pass the ARM64/export audit.
CI is configured to build/audit all eight targets (not yet run remotely).
An exposed non-Mac presenter lint failure is corrected; strict Rust workspace
Clippy now passes for macOS, Linux and Windows. This is not native Windows/Linux
engine validation or runtime coverage for games whose content is unavailable.
The refreshed Episode One/Two fixed-camera Metal map-load/exit gates pass;
Episode Two also exercises split startup/shutdown. The defense-scene speech
report remains unresolved.
The following pack-index increment moves central/local-header validation,
canonical filename lookup and metadata lifetime into Rust, excluding the native
index/hash table from Rust builds. There are 295 passing workspace tests;
10,000 index lifecycles and 134 real-filesystem ZIP/BSP read/seek/enumeration
cases pass. The 119-map corpus also matches index metadata and payload bytes.
Native discovery/range extraction, absolute/filter/pure-server reads and BSP
wildcard filtering remain. This increment rebuilt the shared package and HL2,
not the entire game matrix again; it does not change the public IFileSystem ABI.
The later payload increment also moves file-range I/O, stored/LZMA decoding,
CRC validation and pack cursors into Rust, excluding the two native payload
reader classes and ReadFromPack from Rust builds. File-opened indexes retain a
Rust descriptor; opened payloads survive unmount. A thin native file adapter,
discovery, BSP range extraction, and explicit BSP wildcard filtering remain.
297 workspace tests, 135 native filesystem cases, the installed corpus, seeded
fuzzing and a fixed-camera Metal load/exit check pass. This does not establish
campaign-scale memory/latency, live pure-server policy or speech correctness.
The subsequent archive-discovery increment removes native mount-open handles,
ZIP length seeks and BSP header/range reads from Rust builds. Rust owns typed
ZIP/BSP discovery, timestamp/range reporting and one retained descriptor through
payload reads. 299 tests, malformed/empty BSP and numbered ZIP ordering checks,
the 119-map corpus and fixed-camera Metal load/exit pass. Native candidate path
enumeration, mount selection/trust and explicit BSP wildcard filtering remain.
The next increment moves that pack wildcard filter into Rust, sharing the
ordinary glob policy and fixing root-file/multi-dot/partial-pattern behavior.
301 workspace tests and independent/native wildcard checks pass. The native
adapter only drains results to legacy FindData lists; candidate enumeration,
mount policy and public file/find adapters remain, so this is not full
filesystem retirement.

Numbered ZIP candidate discovery and precedence now also belong to Rust.
The native mount adapter consumes a context-owned snapshot with no native stat
loop, filename generation, or reverse-order policy in Rust builds. First-gap,
malformed-candidate, numeric/localized ordering, cursor lifetime and actual
head/tail/loose-file/reuse tests pass, alongside 304 workspace tests and the
fixed-camera Metal regression. Mount registration/trust and native public
file/find adapters remain. Xbox naming is tested, not Xbox runtime execution.
The all-eight source build/install matrix has now been refreshed after those
filesystem changes, with identical shared-library hashes across the eight
installs. Native pack checks additionally pass 200 map-replacement/remount
cycles, nested map access and retained old-map handles. The isolated episodic
runtime is refreshed; Episode One/Two fixed-camera Metal load/exit gates pass,
including Episode Two split lifecycle cleanup. Speech remains unresolved.

The next increment consolidates pack ownership in Rust: ordinary mounts share
the same metadata and retained file descriptor as explicit pack reads, without
reopening or retaining a second compressed-byte copy. Metadata-only indexes
and in-memory packs keep distinct contracts. Three private C++ range/raw-index
bridges and unused native range mirrors are retired; public Rust range APIs
remain compatible. There are 307 passing workspace tests, cross-platform strict
Rust checks, corpus/ABI checks and a shared-owner sanitizer fuzz pass. The latest
HL2/shared build and all-eight static package audit pass; the full source matrix
above predates this increment. Native mount registration and selection/trust
remain, so full filesystem retirement is not established.

Read-path ID selection now also belongs to Rust and is shared with the native
iterator. The reserved BSP selector selects only typed GAME map packs; every
Rust read/size/resolve/directory/find query follows the same rule. Bounded find
cursors preserve the native basename limit without losing later results, while
BSP text reads retain the CRLF-aware native adapter. There are 309 passing
workspace tests, 320 ABI policy combinations, strict Rust checks across the
three configured platforms, and a 119-map/69,329-entry corpus comparison.
Native ordered mount registration, store-ID deduplication and trust remain.

The following increment moves that duplicate-store state and iterator traversal
into Rust-owned search plans. The native iterator retains resource references
and maps selected Rust indices back to them; fallback native finds use Rust
visit sets too. There are 311 passing workspace tests and independent
randomized ABI/fuzz checks for filter-before-dedup order, replay, signed IDs,
released input memory, concurrency and handle lifetimes. Mount registration,
store-ID assignment, platform exclusion inputs and trust still remain native.

The live ordered mount table and iterator snapshot tables now also belong to
Rust, including lifetime control for temporary native path objects through
clone/drop callbacks. C++ ordered vectors are excluded from Rust builds;
positive store-ID allocation is Rust-owned and checked at exhaustion. Native
physical-path alias matching, duplicate/repositioning decisions, map CRC IDs,
path-ID metadata and trust still remain. There are 314 passing workspace tests
and explicit callback/reentrancy/failed-clone/bounded-table ownership checks;
this is not full filesystem or native resource-adapter retirement.
All eight source targets have been rebuilt after this increment. Episode Two
Metal load/exit also passes; full gameplay remains unverified. The user will
perform manual gameplay checks, while further migration work uses cheap,
targeted verification. See handoff-all-games.md for the 4K Metal launch command.

| Program area | Current evidence | State |
| --- | --- | --- |
| Provenance/reference | Revisions frozen; local Steam corpus discovered; ARM64 play log inspected; timer corrected; legacy unit runner passes repeated ARM64 stress and a full ASAN run; the pinned 1.85.1 toolchain is now enforced at Waf configure time, after a Homebrew Rust ahead of rustup on `PATH` was found silently ignoring `rust-toolchain.toml` and building the engine's Rust library with a different compiler than CI uses; the CI build script also names its own Waf lock, after the stale default lock was found redirecting the build away from the directory it had just configured; the smoke, save-demo, physics, hud-audio and ten-minute gates pass against a build made with the pinned compiler; every startup error the engine raises used to end in a modal dialog, which an unattended run has nobody to dismiss, so any error at all reported as a hang and the reason never reached a log, which is what the intermittent startup stall after the content-ready phase turned out to be: sampling a stalled run's threads caught it blocked in `NSAlert runModal` under `SDL_ShowSimpleMessageBox`, and the launcher now writes the message to stderr under `-nomessagebox`, which the gate passes, and the physics gate needs at least a 300-second budget because its scripted waits alone run past 100 seconds; the physics and soak gates were also reading their samples out of the engine log with a scan that skipped the marker's own line, and since the engine writes a marker and the console output it introduces without a flush between them, the two share a line about as often as not, so a run with working physics failed for want of a position it had in fact reported, which is now read from the marker's line as well; the intro lip-sync gate now passes, and the stalled voice-channel mixer behind it turned out not to be an engine defect at all: its samples used to be gated behind the sentence clock leaving zero, which meant a stalled clock and a still face produced the same empty trace, so the sampling was moved onto the client's own frame clock and the sentence clock is now reported beside each sample instead of deciding whether one is taken; with that in place a run of `gman_intro.vcd` showed the rendered flex weights moving between 34.7 and 37.5 while the viseme magnitude held at exactly 0.7840 and the sentence clock read `elapsed=0.000` for the whole 6.65-second sentence, so G-Man's mouth did move on screen, as was confirmed by eye, but it was the scene's own `face` and `Head` flexanimation tracks posing it rather than the speech; tracing the clock back through the mixer found `SND_MoveMouth8` was never reached, then that `MIX_MixChannelsToPaintbuffer` was running with an empty channel list, and finally that `MIX_BuildChannelList` removes every channel on POSIX while the game is not the foreground application, which an unattended gate never is, so the sounds a run reported starting were being discarded before a sample of them was mixed and the voice channel's position never left zero; the scenarios that assert on sound now run with `snd_mute_losefocus 0`, after which the sentence clock advances to sample 462484 and the viseme magnitude takes 114 distinct values across the sentence, which is the gate's assertion again rather than a reported symptom; the same muting had been hiding itself from the hud-audio gate, whose sound evidence was `S_StartSound` returning a guid and so could not tell a mixed sound from a discarded one, so the mixer now emits `RUST_SOUND_MIXED` the first time it takes a channel's samples and the gate reads that instead, reporting 156 of 193 sounds mixed; no gate captures the pointer any more, so a long run leaves the machine usable; provenance and signing remain open | In progress |
| ABI laboratory | Versioned C ABI, opaque handles, C++ lifecycle owner, caller-owned buffer contracts, panic containment, 10,000-cycle and unload tests | In progress |
| Format foundations | Checked endian/bit primitives plus bounded KeyValues, VPK, BSP, VTF, Studio model, WAV, scene-image, demo, protocol-25 server/client network-message, binary DMX/PCF, and save readers; typed BSP plane/node/leaf and PVS/PAS queries; BSP world surfaces that triangulate into indexed geometry and resolve the material each is drawn with across the texinfo, texdata, and name-table lumps, grouped into one run per material with texture coordinates normalized against each material's own size, leaving out the sky and the compiler's `nodraw`, `hint` and `skip` annotations, checked against shipped content where `d1_trainstation_01` yields 24,005 triangles over 34,993 vertices in 397 runs across 552 named materials; materials that resolve through `patch`/`include` indirection to the one lowercase forward-slashed texture path the archives store, checked against every one of the 5,193 materials Half-Life 2 ships, all of which parse and 4,860 of whose 4,884 named base textures are found in the base game's own archives, the shortfall being developer test materials, HDR skybox variants and legacy HUD sprites; VTF mip levels read out of the smallest-first order the file stores them in; the ZIP archive a map embeds in its pakfile lump, mounted ahead of the game's archives as the engine mounts it, which is the only place the 256 cubemap materials `d1_trainstation_01` uses exist; a map's baked lighting, taken from the high-range lump where the map carries one and read only for the static light style, with each lit surface's samples packed into one image and each vertex carrying where it falls in it, where `d1_trainstation_01`'s 7,783 lit surfaces of 9,033 pack into 2048x297 and every one of them projects inside its own block; a face's displacement read as the sixteen-bit field it is rather than run together with the fog volume stored beside it, which had been reporting every real displacement as none because the compiler writes a fog volume of negative one for an ordinary surface, so the pair read negative either way; the map's brush models, its entity text read as the brace-delimited key and value blocks it is, where `d1_trainstation_01`'s 1,302 entities place all 162 of its brush models past the world and none of them claim the world, and repeated keys are kept in the order the map states them because that is how an entity lists several outputs on one event; the surfaces a point in the map can reach, from the visibility set the compiler wrote, where the set is symmetric across all 122,643 of the map's visible cluster pairs and a player's start reaches 1,416 of the world's 6,829 surfaces; the runs of an index buffer a view can hold, culled by each surface's own bounds rather than by the leaf it is listed in, because on this map surfaces reach as much as 1,440 units past those leaves; protocol-7 demo compatibility; VPK/BSP packers; twelve fuzz targets that CI now compiles and runs rather than only reading the manifest of, which is what caught a splitter that produced no pieces for an empty payload while claiming one; the installed content gate validates 44,725 packed/loose format instances, 40,752 demo packets containing 163,944 messages, and 21 exact declared exceptions, plus four live save containers and 22 map states | In progress |
| HL2 vertical slice | The complete 2,208-task `--rust-engine` graph links on ARM64 and installs a Rust-owned process executable; isolated `d1_trainstation_01` runs exercise Rust paths, ordered mounts/live content reads, scene validation, SDL state, and the Rust host lifecycle through server activation and orderly shutdown; a frame-synchronized acceptance run verifies held-forward player movement, live NPC thinkers and active physics objects, save/reload, a transition to `d1_trainstation_02`, and recording/playback of a demo that passes the Rust parser; the scripted movement endurance gate ran for 611 seconds and shut down cleanly; a sound-enabled G-Man intro gate verifies 40 decoded phonemes, nonzero viseme/controller output, final model flex output, and visible mouth-shape changes; an interrupted active test script now closes before filesystem unload and exits cleanly; a direct physics-interaction gate on `d1_trainstation_02` spawns a named crate, requires it asleep, and requires it awake with nonzero velocity after the player punts it, having first asserted two distinct in-game player positions; a HUD and audio gate on `d1_trainstation_02` requires a live audio device rate, mixed gameplay sound, an SMG shot reaching the mixer, the ammo element counting a clip down, sprinting draining suit power, and damage moving health, and last passed with 225 sounds started, the clip at 45 then 18, and health at 100 then 88; a 60-minute soak ran the engine to 3690 seconds, hit its start, both movement-sample and completion markers, and exited cleanly after 3658 seconds, and was re-run after the sound scenarios were found to be discarding every channel while out of focus, passing again in 3646 seconds with sound genuinely mixing throughout; the long gates now run detached, in their own session rather than as a child of the shell that starts them, after the previous backgrounding was found to kill them when that shell exited, which is what left an hour-long run with an empty log and made every long gate block the session that started it; runs have been observed stalling during startup immediately after the `content ready` phase, producing no engine log at all and never reaching their test script, with the identical invocation against the identical build passing on a later attempt, and eight consecutive direct launches of the same build reached the engine log in two seconds each, so an intermittent startup hang is open and unexplained rather than ruled out; the smoke harness now samples the hung process's threads once, after 45 seconds without an engine log, and reports the sample path on failure, so the next occurrence yields a stack rather than another lost run; the same tree configured without `--rust-engine` activates its server 6.4 seconds in and holds the map for a full minute, so the native C++ ARM64 build does reach playable content and can serve as the comparison oracle Phase 1 requires — an earlier record here claimed it exited during map spawn, which was an artifact of launching it without `DYLD_LIBRARY_PATH` pointing at its own `bin`, not a property of the build | In progress |
| Rust host | Rust owns the native macOS process entry and exact argument storage, loads the executable-relative legacy launcher as one transitional callback, and retains cached VPK indexes, the selected game's `gameinfo.txt`-ordered `GAME` search that is rebuilt in exact native loose/VPK/ZIP/BSP order after search-path changes, by-request-only visibility and trusted runtime-symlink semantics, direct relative existence/size/directory queries, context-owned precedence-ordered wildcard cursors, and context-owned streaming loose/CRC-validated VPK/ZIP/BSP read handles across synchronized path IDs, a synchronized ordered loose-directory write registry with canonical relative target resolution and Source-compatible `GAME_WRITE`/`MOD_WRITE`/default fallbacks, context-owned relative writable handles with open/read/write/flush/seek/tell/size/close operations, relative recursive directory creation, file removal, rename, and writability query/change, stateful input services, an owned BSP world with point/PVS/PAS queries, a bounded command queue and case-insensitive cvar registry, per-channel incoming/outgoing network sequence and packet-drop decisions, outgoing packet-header encoding/finalization and folded CRC generation plus bounded incoming header/choke/challenge validation, the `SPLITPACKET` header layout the engine now writes on every outgoing piece and per-peer reassembly of incoming ones that places each piece by the offset its own header declares, refuses a short middle piece, a piece that disagrees with the split size its message started on, or one naming a position outside its own count, counts a duplicate once, abandons a message the sender moved on from, and holds a finished message rather than losing it to a caller's undersized buffer, with `sizeof(SPLITPACKET)` and `NET_MAX_MESSAGE` asserted at compile time against the constants Rust rebuilds to, checked LZSS and Source-tagged Snappy generic-buffer codecs without native error fallback, opaque Rust-owned ZIP/BSP filename/metadata indexes used by the native pack adapter, canonical network string-table entries/indices/user data/change ticks/rollback history, canonical server datatable descriptors/compatibility CRC/server-class IDs, immutable server-frame snapshot entity/serial/class ordering and explicit-delete state, per-receiver packet-entity delta classification that decides creation, removal, recreation and delta candidacy from visibility-narrowed snapshot identities, protocol-25 per-entity delta header bit encoding, the string-table entry bit encoding demo and save containers carry, plus decoding of the string-table sections a recorded demo carries so a recording is only reported valid when its tables read back, map-state and leading save-container composition that both refuse anything the Rust save reader would reject, a validated launcher/content/run/shutdown lifecycle, and ownership of synchronous and split-phase app-system startup/reverse-order rollback and the outer legacy-session/restart loop, the active non-dedicated message/frame iteration loop, render-frame ready/wait pacing, simulation tick accumulation/remainder/alternate-tick decisions, and the single pending new-game/load/change-level/shutdown operation; every legacy server map spawn synchronizes and validates the Rust world, and frame boundaries drain Rust-owned commands into transitional executors; C++ still owns individual subsystem callbacks, module IO, absolute/pure-server file paths and enumeration, archive discovery and range extraction, absolute/pure-server/filtered archive reads and explicit BSP enumeration, reliable fragments/message dispatch, pointer-stable string-table mirrors/callbacks and the client-side entry section, datatable proxy callbacks, pointer-bearing snapshot mirrors, entity packing/change detection/payload delta emission, pumps platform messages, performs the requested platform sleep and legacy tick/map operation bodies, integrates Hammer, consumes its own BSP state, and executes console commands | In progress |
| Presentation/simulation | Legacy modules still own all systems; the retained facial-animation path now has a sound-enabled intro regression gate | Not started |
| Required toolchain | Initial VPK/BSP CLIs only | In progress |
| TF2 implementation | No authorized gameplay implementation yet | Not started |
| Bridge retirement/platforms | Transitional bridge introduced; no legacy bridge retired; the Rust workspace itself compiles, lints, and tests clean for Linux and cross-checks clean for `x86_64-pc-windows-msvc`, both gated in CI, which is the platform groundwork rather than a ported engine | Not started |
| Metal renderer | Native Apple-silicon Metal renderer with higher-quality output; sequenced by the plan after the Rust-owned host and before bridge retirement, with scope and gates recorded in `metal-renderer-roadmap.md`; a `source-render` crate now opens the system Metal device and one command queue through a hand-written Objective-C runtime binding that adds no external dependencies, and runs a windowless render pass that clears a shared-storage BGRA target and reads exact pixels back, verified on an `Apple M5 Pro` where a green clear returns `[0, 255, 0, 255]`; it compiles Metal Shading Language at runtime, builds render pipeline state, uploads vertex buffers and sampleable textures behind handles that release on drop, and draws through them, verified by sampling a four-texel texture across a covering triangle and reading each quadrant back exactly; textures accept the BC1/BC2/BC3 blocks HL2's VTFs are stored in and upload them unchanged, verified by sampling a hand-built BC1 block and reading its exact decoded color; a whole mip chain uploads as the material author shipped it rather than being regenerated, verified by reading a red base level and a green second level back through explicitly levelled samples, and a chain whose levels do not match their own sizes or that runs past the one-pixel level is refused; index buffers take the 16-bit entries Source stores, verified by drawing a covering quad from four shared corners and six indices and reading back the same image the covering triangle produces, and the indices are checked against the vertices they select from before the draw is encoded because an out-of-range index faults the GPU rather than drawing the wrong thing; a `CAMetalLayer` swapchain presents frames and captures the drawable that was presented rather than a parallel render, and honours resize and Retina scale independently, verified by presenting into an 8x8 drawable from a 4x4-point surface at scale 2 and reading the presented pixels back; a pass takes as many draws as it has surfaces rather than one, and refuses a pass that mixes pipelines writing depth with ones that do not because Metal fixes that attachment for the whole pass; world geometry is placed on screen by a camera transform stated in Source's own conventions and mapped onto Metal's `0..w` depth range, with a depth buffer deciding which surface is in front, verified by a wall covering exactly the middle half of the view at the distance and field of view that predicts, and by a nearer wall winning over a farther one in either draw order; a draw reads its own run out of an index buffer rather than the whole of one, verified by drawing each of two runs separately and seeing only the wall the run names, and a run reaching past the buffer is refused; per-vertex texture coordinates bind as their own buffer, verified by sampling a two-colour texture across a wall and reading each half back on the side the coordinates place it; the BSP and renderer halves are checked against each other on shipped content rather than synthetic geometry, where `d1_trainstation_01` triangulates into 24,854 triangles over 13,329 shared positions and fills the whole view rendered from a point the BSP reports as open space, which is skipped with a message where no Half-Life 2 installation is present; the map's materials and textures now load and bind through the same search path the engine builds, where 394 of `d1_trainstation_01`'s 397 materials bind a texture totalling 57.8 MiB and the resulting view comes out in 26,296 distinct shades, the three that do not being the camera feeds the engine renders into rather than loads; a draw takes a second texture and a second per-vertex coordinate buffer so the material can be modulated by the surface's baked lighting, and every per-vertex attribute now states its own stride so a buffer holding fewer values than the draw has vertices is refused rather than read past, which was previously unchecked; the world draws lit from the map's own lighting lump, packed into one image per map, established not by the frame looking plausible but by drawing the same geometry through a pipeline that ignores the lighting and comparing the two, where across twenty-four views from six places in `d1_trainstation_01` the lighting scales the world between 0.00 and 0.94 and darkens most of what it touches; the renderer now reaches the engine's own window, where it previously drew only into a layer of its own and was read back offscreen, and where this used to record that attachment waited on the ToGL hand-off, which was circular since nothing would schedule that hand-off until something drew into the window; under `-metal` the window is created without `SDL_WINDOW_OPENGL`, no GL context is made for it, and the Rust renderer attaches a `CAMetalLayer` to the window's own view through four new ABI entries, reporting `points=1280x720 drawable=2560x1440` on `d1_trainstation_01` with the backing scale read off the window rather than passed in, so a window dragged between displays of different scale redraws at the one it is on; the window is handed across rather than its view so the C++ side never sends an Objective-C message, and presenters are held per thread rather than in a shared map because AppKit requires view changes on the main thread and Metal's objects are not `Send`, a handle used from another thread being reported invalid rather than made to work; this does not yet draw the game: with no GL context the engine reaches `CShaderDeviceDx8::CreateD3DDevice` and faults inside `GLMContext`'s constructor, which is ToGL building the context it translates Direct3D into, so the wall is in the component this work exists to delete rather than anywhere around it, and the next step is giving the material system a Metal device to talk to; the default path is untouched, creating its GL context exactly as before, and the smoke gate passes on it; the Metal path is tested against a real GPU in the macOS arm64 CI job and refuses to pass by skipping on Apple silicon; platforms without a backend refuse explicitly and that path is compiled, linted, and tested for Linux and Windows; a frame now draws what can be seen from where it stands rather than the whole map, reading the map's own visibility set for what a wall hides and each surface's own bounds for what the screen does not hold, which across twenty-four views from six of `d1_trainstation_01`'s own player starts and entity positions draws 4.6 percent of the map's triangles on average and 29.2 percent at worst, in 2,215 draws rather than 9,528; that it costs nothing visually is established by rendering each view twice, once culled and once whole, and requiring no pixel to move by more than one step, a bound rather than an exact match because drawing the same triangles in the same order as more calls than before already shifts the odd pixel by one, which was measured rather than assumed; culling by the leaves a surface is listed in was tried first and abandoned, because on this map surfaces reach as much as 1,440 units past those leaves and the holes that opened were real; the map's other 162 brush models are now drawn where they stand rather than heaped on its origin, each being a door, lift, breakable or trigger volume the compiler cut out of the world and stored about an origin of its own, with the entity that owns it carrying the origin and angles that put it back and a model whose entity gives no origin already being in world coordinates, which the same transform covers by reading as the identity; only the position is transformed, because both the texture and the lightmap projections are defined against the surface as compiled, so a placed door keeps the texture and baked light it was built with; which models are stored local was read off the map rather than assumed, since a local origin is not the middle of the brush but a train's end or a door's hinge, so the test is the leaf each model's middle lands in, where placed 158 of the 162 stand in a room the visibility set gave a cluster to against 26 stored; a placed model is culled by the leaf it stands in rather than by the leaves listing it, which list it at the origin, and leaving them all to the view frustum instead doubled what a frame drew to 9.3 percent of the map's triangles; this brought 2,204 surfaces, a quarter of the map's total, from the origin to where the map puts them; the props a map is dressed with now draw as well, which is the first geometry from outside the map file to reach the screen, with each static-prop dictionary entry loaded once from the three checksum-matched files the compiler splits a model across and placed as many times as the map places it, where 284 of `d1_trainstation_01`'s 298 placements over 82 of its 86 models draw, the rest naming models the base game does not ship, coming to 164,447 triangles in 50 material runs every one of which binds a shipped texture; joining the three files is where the quiet mistakes live, because each mesh's triangles are numbered from that mesh's own base within the model's single run of vertices, so losing the base draws a prop out of another prop's triangles rather than failing, and the placements are held against each model's own reach rather than a fixed distance since a lamp post stands a long way above the point it is placed at, with 279 of the 284 more than 512 units from the map's origin so a dropped placement could not pass; the frames are checked for the shape a right answer has rather than for coverage, since props stand about a map rather than covering it, so all four views from the player's start hold some without any being filled edge to edge, which is what geometry heaped around the player's feet would draw, and the best draws 8,852 distinct shades where a handful would be the flat fill a lost coordinate buffer leaves, which is how a swapped pair of shader buffer indices was caught; only the static light style is applied, so lights that switch or flicker are held at their baked state; every model's skeleton now composes from the root down and is held to the one statement the file makes about itself that needs no second source, that the compiler wrote each bone's transform out of model space as the inverse of where that bone sits at rest, so the two must cancel and leave every vertex alone, which is checked across all 2,201 models the base game's archives list rather than a handful, 2,195 of which compose over 13,197 bones and thirteen joints deep at the most, all cancelling to within 7.3e-4, the other six being names the archives hold nothing under and asserted empty rather than skipped; this also corrected a wrong reading, that a jointed model's vertices were stored against its bones: they are in model space in the rest pose like a static prop's, and what differs is that the box in a jointed model's header is its movement hull rather than a drawing bound, so a person's arms reach past their walking box; posing from animation data, which is what the composed skeleton is for, is not done; props are lit by the map rather than drawn fullbright, from the light the compiler measured arriving at points inside each open leaf from six axial directions, shaded by whichever of those a surface faces weighted by the square of each component of its normal so that turning a surface neither brightens nor darkens it, which Half-Life 2's own maps keep inside each leaf record rather than in the lumps a later compiler writes it to, so reading only those lumps finds them empty and lights every prop in the shipped campaign black; both are read, the later lumps preferred where a map has them and the high-range pair over the standard one, and a draw now takes a colour per vertex for it, which is also how a player, a thrown crate and any `$vertexcolor` material are lit; reading it wrong yields no picture to look at, since a cube whose faces are in the wrong order or whose shared exponent is applied wrongly still shades a prop to some plausible grey, so it is checked against what must be true of light in a room whatever the room is: of `d1_trainstation_01`'s 5,353 leaves 5,283 carry a measurement, 3,989 of the 5,242 that differ top to bottom are brighter above than below, and the peaks run from 8.4e-9 through a median of 6.8e-3 to 1.23, a spread of eight orders of magnitude a constant or a mishandled exponent does not produce; that this is what changes the picture is established as the world's lighting was, by drawing each view through pipelines differing only in whether they modulate by it, where all 51,828 prop pixels across four views change and all darken; this is the ambient term only, since Half-Life 2 shipped before the compiler baked per-prop vertex lighting and `d1_trainstation_01`'s archive holds none, so the map's direct lights are added at runtime by the engine and not yet here, leaving props dimmer than the world they stand in; the renderer now draws the running game with no OpenGL device anywhere in the process, where the previous entry recorded it faulting inside `GLMContext` for want of one, the answer being that the engine's need for a graphics device and its need for one that draws are separate concerns and only the first has to be met for the game to run: `-metal -noshaderapi` has the launcher load `shaderapiempty` rather than `shaderapidx9`, so ToGL is never loaded, no GL context is created, and the window carries a `CAMetalLayer` instead of a GL surface, and the engine's frame loop draws the map through the Rust renderer where the old renderer drew, once per frame with the view it has just set up; the map's geometry, materials and packed lighting are loaded onto the Metal device once and each frame selects from them by the map's own visibility set and the view's bounding planes, which on `d1_trainstation_01` is 23,999 triangles across 397 bound materials of which a frame draws between 445 and 7,686 depending on where the player stands; the seam is two calls wide, the engine handing in a position and a set of angles and getting back a presented frame, saying nothing about batches, lightmaps or culling and being asked nothing about entities or game state, and the world renderer itself was moved out of the test that proved it into the crate so that the engine can reach it at all; the level file name was the wrong thing to key the map on, being what the client shows on its scoreboard and empty until it has parsed the server info, so the server's own world model is used instead, which is set as the level loads; what a run drew can be kept with `SOURCE_METAL_SHOT_DIR`, which writes the presented drawable's own pixels rather than a second render of the same view, and `d1_trainstation_01` comes out as a lit, textured station concourse; this also caught that a Homebrew Rust ahead of rustup on `PATH` silently ignores `rust-toolchain.toml`, which had let `cargo clippy --fix` introduce `is_multiple_of` and `as_chunks` into `source-bsp`, neither of which exists on the pinned 1.85.1 and both of which failed only in the Waf build; the direct-physics gate now passes on this path as well as on the legacy one, the player walking from `-4304 -224 -62` to `-4304 -278 -64` and the spawned crate going from asleep to awake with a velocity of `-2.15 4.14 -4.31`, and audio is unaffected, 103 of 151 sounds mixing including 27 shots of the SMG; that gate had been failing here, and neither the renderer nor the shader API was the reason: the scenario waited a fixed twenty-five seconds after its `changelevel`, which outlasts the nineteen-second transition the legacy renderer takes but not the forty-three seconds it takes with no shader API, so `getpos` reported the previous map's spawn twice over and `+forward` moved nothing, which reads as a physics failure on a run where physics was never reached; the scenarios now wait on the `FinishedMapLoad` checkpoint the client emits once it is fully connected rather than on a guessed duration, so they no longer assume how long a configuration takes to load; finding that also turned up a gate that could not fail, since the physics and soak movement checks compared the matched log line from `setpos_exact` onwards and the engine's own timestamp is inside that span, so two samples always differed however far the player had moved, and a run in which the player never moved at all passed; both now compare only the coordinates, as the acceptance scenario already did; the map's props are now drawn in the running game rather than only in a test, `d1_trainstation_01` loading all 284 placements it names and the frame going from 23,999 triangles of world to 188,446 of world and props, each prop transformed into world space once at load rather than per frame and lit per vertex from the ambient cube of the leaf it stands in, and culled by its own world-space bounds rather than by the point that places it, since a lamppost's geometry stands a long way above that point; that also turned up a defect worth naming, because the first frames captured with props were filled edge to edge by a surface reading `TRIGGER`: a map is built with surfaces meant to be reasoned about rather than seen, the volumes that fire triggers and the planes that stop a player but not a bullet, and they carry materials under `tools/` which the engine draws none of, and they are both among the largest surfaces in a map and placed around the player rather than in front of them, so drawing them did not add a stray detail, it hid the map behind it; their materials are no longer bound, which is what skips them, since a batch whose material never resolved is already dropped; the interface now has a path to the screen that does not go through the shader API: `source-render` grew a blended pipeline, `source-materialsystem::Overlay` gathers screen-space rectangles and composites them into the same pass as the world so they sit over it rather than replacing it, and five ABI entry points carry them across, with VGUI forwarding from `DrawQuad` and `DrawQuadArray` ahead of the mesh check, since with no shader API there is no dynamic mesh and the quad would be dropped before the renderer that can draw it ever saw it; fonts arrive as pixels through `DrawSetTextureRGBA`, which is how VGUI hands over a rasterised character sheet, so nothing in Rust has to shape text; the engine no longer skips painting outright under `-noshaderapi` when the Metal renderer is attached, and the effect is measurable rather than assumed: quads reach the renderer each frame and `RUST_HUD_HEALTH` now fires where previously no HUD element ran at all; it is not finished, and the two things left are specific: no interface pixels have yet been confirmed on a captured frame, the quads seen so far being the intro's black letterbox bars, and the values the elements report are wrong under this configuration, health reading `value=0` four times and then stopping and the ammo element never reporting at all because it has no active weapon client-side, while the server fires the SMG and mixes 27 of its shots, so the client's entity state rather than the drawing is what to look at next; tracing each layer of the frame since then rules most of them out, because `V_RenderView` runs every frame with a renderable world, an active client and no lost video memory, the client's own `RenderView` is entered eighteen times a second with `RENDERVIEW_DRAWHUD` always set, `CEngineVGui::Paint` is called just as often with both the interface and the in-game panels, and the screen measures 1024x768 with the client's root panel the same size and visible, yet the HUD's elements think four times in a run and find no local player each time; one real defect fell out of that, `shaderapiempty` having reported a window of no size at all, which was safe while that device existed only so the engine could run with nothing on the screen but is not once something else draws, since VGUI sizes itself from that answer and zero collapses every panel to nothing with no error anywhere, a panel of no size having nothing to fail at, and it now reports the size it was set to; the obvious next suspect is ruled out rather than confirmed, because that device also answers that it is not using graphics, which skips material precaching and texture upload entirely, and answering yes under `-metal` made matters strictly worse, the health element then reporting nothing at all rather than four times, so material precaching is not the missing piece and that avenue is closed rather than left to be tried again; a second real defect came out of the same search, the SDL window's rendered size having been latched only from inside ToGL, so with no OpenGL context it stayed at the zero it starts as, and that zero does not stay put, because the engine sets the material system's viewport from it every frame and sizes VGUI's root panel from that viewport, leaving the whole interface laid out inside a rectangle of no size; the window knows how big it is whoever draws into it, and now says so; neither fix moved the HUD, and tracing the health element itself rather than the frame around it says why. An earlier reading here concluded the client had no local player, which was drawn from a sample taken only during the level transition and is wrong: asked once a second from the client's own frame, the client is fully populated under `-metal -noshaderapi`, holding 649 entities, a local player, an active `weapon_smg1` and a health of 88 after the scenario's 25 points of damage, so the simulation this configuration runs is the same one the legacy renderer runs and the gate's own subject matter is present. What is false is the element's own predicate: `CHudHealth::ShouldDraw()` answers no on every frame, so the panel is never made visible, an invisible panel is not recursed into, and a panel that is not recursed into is never thought, which is why the element appeared to think a handful of times rather than continuously. That predicate is `CHud::IsHidden` against the element's `HIDEHUD_HEALTH | HIDEHUD_PLAYERDEAD | HIDEHUD_NEEDSUIT`, and each of its terms can now be decided rather than guessed at: the player's `m_iHideHUD` reads 2176, which is `HIDEHUD_BONUS_PROGRESS | HIDEHUD_CHAT` and intersects none of the element's bits, and the player is alive, which leaves `HIDEHUD_NEEDSUIT` as the only term that can be failing and says the player is not wearing the HEV suit. The scenario asks for it with `impulse 101`, and that impulse is thrown away by the engine before it can be acted on. Traced from both ends in one run, the client latches it into a usercmd and the server receives that usercmd carrying it, so nothing about the console command, the network path or cheats is at fault; what discards it is the guard at the top of `CBasePlayer::PlayerRunCommand` that zeroes a command's movement and impulse while `developer` is 0, the load is a new game and the server's clock is under three seconds, which is the engine holding a player still for a moment after a level starts. The same script loses the impulse on one renderer and not the other because the two reach that line at different points on the server's clock: the impulse arrives as command 1584 at `curtime` 6.0 with the legacy renderer and as command 46 at 1.4 without a shader API, so the window is shut in one case and open in the other. The checkpoint the scenario waits on does not close it, because it reports that the client is connected rather than that the server has begun simulating, and with no shader API the server starts ticking about ten seconds after it; waiting longer there does not help either, since the server's start slides along with the wait. The wait therefore has to sit immediately before the impulse, where the clock is already known to be running, and with it the HUD/audio gate now passes under `-metal -noshaderapi`: the health element reports 0 then 88 across 1,912 samples, the ammo element 1,908, the SMG's clip runs 45 down to 18, and 127 of 206 sounds mix, against 169 of 220 and the same clip and health on the legacy renderer, which still passes unchanged. This is the class of defect the port keeps turning up rather than a one-off: an authentic engine behaviour that a scenario was passing only by accident of timing, which a slower configuration then exposes. What the HUD draws is not yet what it should, and the captured frames say so exactly: the world comes out lit and textured with its props, and a single untextured white rectangle sits in the middle of it, which is the overlay path putting real interface geometry on the Metal drawable for the first time while the textures those quads name are still missing, since the empty device answers that it is not using graphics and so never uploads them; the elements now run and report true values, and giving their quads the right pixels is what remains. Three things that were wrong about the path are now right and one thing that is wrong is now named. The engine makes two texture identifiers for every font sheet, one drawn normally and one additively, and hands the pixels to only one of them, saying which is which through `ReferenceProceduralMaterial`; the HUD's own text is the additive one, so every glyph it drew named a sheet that had never been given anything, and that statement is now forwarded and resolved when a rectangle is drawn rather than by copying, so a later change to the sheet reaches every name for it. A font sheet is also not rasterised once: the engine draws each glyph into it the first time that character is asked for and says so through `DrawSetSubTextureRGBA`, so a sheet handed over whole held only the characters that happened to be needed when it was made, and a HUD counting down from a hundred would have lost its digits as it went; those updates are now composed against the sheet this module keeps and sent back. The window the quads were gathered in was wrong too, having been opened at the view and closed at the present, which catches only the paint that falls inside it; gathering now runs between one present and the next, which is the whole of what the interface last drew. None of that put the HUD on the screen, and the reason is now measured rather than suspected: of 2,826 glyphs a second that the interface asks to draw, 2,826 are discarded by `ClipRect` before they reach the renderer, none of them for want of a glyph. The numbers say why. A character of the HUD's text lands at `-64,570` through `-57,589` while the panel painting it is scissored to `0,441` through `640,480`, so the text is drawn outside the panel drawing it, and that panel's own origin is at `-80,441`, which is off the left of the screen. Both are the signature of a layout computed against one screen size and painted against another, and walking the panel tree names the size rather than inferring it: every panel in the chain reports the size it should, `HudHintDisplay` at 768 wide inside a viewport and a client root both 1024x768, while the clip rectangle handed down to them is `0,336..640,480`, so the mismatch is not in any panel but between the tree and the answer the surface gives for how big the screen is. The engine's own mode is 640x480 at the moment it builds that tree, and it says so consistently, the mode, the UI size and the surface all agreeing; what disagreed was `shaderapiempty`. Fixing the `0x0` window earlier had given that device a built-in default of 1024x768 to answer with, which replaced a wrong answer with a different wrong answer: the render context falls back to the back buffer whenever no viewport is pushed, VGUI takes that as the screen, and Half-Life 2 sizes its HUD in proportion to it, so the elements laid themselves out for a screen half again as large as the one they were painted into and every glyph fell outside it. The device is now told the size at `CShaderAPIEmpty::SetMode`, which is the call the material system actually makes, rather than at the device manager's `SetMode`, which is never reached on this path, and it invents nothing when it has not been told. Two smaller faults fell out of the same search and are fixed with it: `CVideoMode_Common` never initialised `m_nUIWidth`, `m_nUIHeight` or the stereo pair, though `GetModeUIWidth` is what the engine sizes its root VGUI panel from, so any read before the first mode was chosen returned whatever the allocation held; and the overlay was uploading its rectangles against the drawable's size when they are stated in the surface's, which on a Retina display are not the same and drew the whole interface into a corner at the ratio between them, so the engine now hands the viewport across explicitly. The measured effect is that panels no longer land off-screen, the negative origins are gone, and the rectangles reaching the renderer went from 6 a frame to 214-239, with interface geometry compositing over the world on captured frames for the first time; the HUD/audio gate now passes under `-metal -noshaderapi` reporting health `100 -> 88`, which is what the legacy renderer reports rather than the `0 -> 88` this path gave before, so the two configurations now agree on the gate's own subject matter. What is still not right is that the text is not yet legible on a captured frame, the geometry arriving as translucent panel backing rather than glyphs, so the font sheets' own pixels and coordinates are the next thing to look at rather than the layout; that the world, its lighting and its props draw correctly through Metal throughout says the renderer is not what is wrong; the HUD/audio gate still fails here on ammo it never rendered while every sound assertion in it passes; particles and models are not drawn either, and the engine's own level-load screen is still the old renderer's | In progress |
| Direct3D 9 on Metal | The route by which the game is actually drawn through Metal, adopted 2026-09-19 in place of the renderer in the row above, which drew the map and its static props and would have needed every other renderer feature written again behind it. `-metal` loads `shaderapimetal`, which is `shaderapidx9`'s own sources compiled against `tometal/`, Direct3D 9 classes that keep the state machine as one plain block and hand draws, clears, copies, locks and presents to `rust/crates/source-d3d9`; that crate turns them into Metal through a hand-written Objective-C binding with no external dependencies and translates the shipped `vs_2_0`/`ps_2_b` bytecode to Metal Shading Language, which was checked over all 1,325 shaders five maps create, every one translating and compiling. Nothing above Direct3D changed, so everything the material system draws is drawn: menus, console, VGUI and HUD text, lit and bump-mapped world, displacements, props, skinned and flexed characters, view models, vehicles, water, fog, sky, particles, decals, the flashlight, and occlusion-query glows. Frames taken at the same place through ToGL and through this on `d1_canals_03` and `d2_coast_01` are the same picture apart from ToGL's multisampling. The smoke, direct-physics, HUD/audio and save/demo gates pass with `EXTRA_LAUNCHER_ARGS="-metal"`, and an existing `d1_canals_07` autosave made by the legacy build loads and plays. `-nativeres` renders at the display's own pixel count, 3024x1898 full screen on a 14-inch MacBook Pro where SDL reports a 1512x982 desktop, at about 110 frames a second on an M5 Pro, with no OpenGL context in the process. Not done: multisampling, and a cache of compiled shaders and pipelines across runs. Design, checks and gotchas are in `d3d9-metal.md`; `scripts/install_macos_metal_runtime.sh` makes a playable folder beside an existing game folder without writing to it | Playable |
